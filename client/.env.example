# API base URL.
# Leave unset in development — Vite proxies "/api" to the server (see vite.config.js).
# In production, point this at your deployed backend, e.g.:
# VITE_API_URL=https://your-api.example.com/api
